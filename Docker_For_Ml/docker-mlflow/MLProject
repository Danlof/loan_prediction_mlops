name: project_wines_quality

docker_env:
  image: wines_quality_image:latest

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python3 train.py --alpha {alpha} --l1_ratio {l1_ratio}"